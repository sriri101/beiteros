{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/", "destination": "/api/detect" },
    { "source": "/((?!assets/|api/).*)", "destination": "/index.html" }
  ]
}
